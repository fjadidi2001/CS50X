#include <stdio.h>
#include <stdlib.h>

int main() {
    char text[] = "12345"; // Replace this with your input text
    int decimalValue = atoi(text);
    
    printf("Text: %s\n", text);
    printf("Decimal Value: %d\n", decimalValue);
    
    return 0;
}
