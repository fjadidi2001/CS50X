#include <stdio.h>
int main()
{

    char name[20];
    puts( name);
    if (name == "fateme")
    {
        printf("%s is string", name);
    }
    else
    {
        printf("%s is another datatype", name);
    }
}  
